import * as React from 'react';

import '../styles/global.css';

const pageStyles = {
    margin: '0px auto',
    maxWidth: '723px',
};

const Splash = () => {
    return (
        <div>
            <title>omakase</title>
            <div style={pageStyles}>
                DASHBOARD
            </div>
        </div>
    );
};

export default Splash;
